<section class="page-title compact-title">
  <p class="eyebrow">Оформление</p>
  <h1>Заказ с доставкой и монтажом</h1>
</section>

<section class="checkout-grid">
  <% if (product) { %>
    <div class="checkout-product">
      <img src="<%= product.image %>" alt="<%= product.title %>">
      <div>
        <p class="eyebrow"><%= product.brand.name %></p>
        <h2><%= product.title %></h2>
        <p><%= money(product.price) %> + монтаж от <%= money(product.installPrice) %></p>
      </div>
    </div>
    <form action="/orders" method="post" class="smart-form">
      <input type="hidden" name="productId" value="<%= product.id %>">
      <label>Имя<input name="customer" required></label>
      <label>Телефон<input name="phone" required></label>
      <label>Email<input type="email" name="email"></label>
      <label>Адрес монтажа<input name="address" placeholder="Минск, улица, дом"></label>
      <label class="check"><input type="checkbox" name="install" checked> Добавить монтаж</label>
      <button class="button button-primary" type="submit">Перейти к оплате</button>
    </form>
  <% } else { %>
    <div class="empty-state">
      <h2>Выберите модель в каталоге или перейдите в корзину</h2>
      <div class="hero-actions">
        <a class="button button-primary" href="/catalog">Открыть каталог</a>
        <a class="button button-outline" href="/cart">Корзина</a>
      </div>
    </div>
  <% } %>
</section>
