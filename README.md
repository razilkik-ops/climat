# Minsk Climate

Современный сайт для продажи, подбора и установки кондиционеров в Минске.

## Стек

- Node.js + Express
- PostgreSQL
- Prisma ORM
- EJS templates
- Vanilla JS для сравнения, квиза и калькулятора

## Запуск

1. Скопируйте переменные окружения:

```bash
cp .env.example .env
```

2. Запустите PostgreSQL:

```bash
docker compose up -d
```

3. Установите зависимости, примените миграции и заполните демо-данные:

```bash
npm install
npm run prisma:migrate -- --name init
npm run prisma:seed
```

4. Запустите проект:

```bash
npm run dev
```

Сайт откроется на `http://localhost:3000`.

## Демо-доступ в админку

- URL: `/admin/login`
- Email: `admin@climate.by`
- Пароль: `admin123`

## Что уже реализовано

- каталог с фильтрами по бренду, типу, площади, цене, инвертору и Wi-Fi;
- карточка товара и заказ с монтажом;
- сравнение до 4 моделей через localStorage;
- квиз-подбор с сохранением результата и заявки;
- калькулятор стоимости с API-расчетом;
- страницы монтажа и гарантии;
- CRM-панель: заявки, статусы, заказы, оплаты, товары;
- демо-оплата с моделями `Order` и `Payment`.

